# iOS 및 안드로이드 앱 리뷰 크롤링

## 기능
- 최대 500개의 리뷰를 받아올 수 있음. 
- ios_app_id 변수를 수정하여 앱을 설정할 수 있음. 
- country 변수를 수정하여 나라를 설정할 수 있음. 
- .csv 파일로 저장할 수 있음. 

## 필수 설치 목록
```
$ pip install xmltodict
$ pip install pandas
$ pip install requests
$ pip install google_play_scraper (안드로이드용)
```